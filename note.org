FREQUENCY: iteration or total

TODO:
 - fix frequency;
 - save seed
 - add config struct and factory
 - add concepts constrains on frequency
 - save seed
 - wrapper master class
 - config file

tofix:
 - config enum
 - config lgn elements repetitions

CHECK: list performace: insert lgn in a list container so the there is no need to realocate all the elements that were after position to their new positions.

