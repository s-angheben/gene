#include "writer.hpp"

#include <chrono>
#include <string>

// template <ranges::common_range T1>
// bulk_writer<T1>::~bulk_writer() {}

// string a_create_tile_file_name(string file_prefix, int bulk_counter) {
//   auto p1 = std::chrono::system_clock::now();
//   return file_prefix + "_" +
//          to_string(std::chrono::duration_cast<std::chrono::seconds>(
//                        p1.time_since_epoch())
//                        .count()) +
//          "_wu-" + to_string(bulk_counter) + "_tile.txt";
// }
// string create_tile_file_name(string file_prefix, int bulk_counter) {
//   auto p1 = std::chrono::system_clock::now();
//   return file_prefix + "_" +
//          to_string(std::chrono::duration_cast<std::chrono::seconds>(
//                        p1.time_since_epoch())
//                        .count()) +
//          "_wu-" + to_string(bulk_counter) + "_tile.txt";
// }
